# 玄同 Xuantong 项目交接文档（HANDOVER）

> 写给接手开发者：这份文档的目标是**诚实**。
> 这里不会美化现状。你会看到一个结构清晰、测试全绿的骨架，但距离「能给真实患者用」还有相当距离。
> 请在动手前完整读完本文，尤其是 [§3 真正需要做的](#3-真正需要做的按优先级) 和 [§5 已知坑点](#5-已知坑点)。

---

## 1. 项目概述

- **定位**：面向基层医疗的「数字化家庭医生团队」AI 系统。
- **核心理念**：用多个 AI Agent 模拟真实的家庭医生签约团队（家庭医生 + 护士 + 公卫医师 + 药师 + 助理）协作。
- **业务主线**：

  ```
  患者事件上报 → 智能分诊 → 多 Agent 会诊 → 风险分级 → （必要时）人工审核
            → 任务生成与执行 → 服务结果记录 → 时间线归档（闭环）
  ```

- **技术底座**：FastAPI（服务）+ LangGraph（工作流编排）+ 阿里云百炼 DashScope Qwen（LLM）+ SQLAlchemy（持久化）。
- **仓库**：`https://github.com/LHT666-hub/xuantong.git`，主分支为 **`master`**（注意：不是 `main`）。

---

## 2. 已完成的（架构骨架层）

以下能力**已实现并有测试覆盖**，但请注意「实现」≠「生产可用」：

- ✅ **FastAPI 服务框架** + 16 个 REST API 端点（其中 `care-teams`、`health-records` 为占位返回空）。
- ✅ **LangGraph StateGraph 工作流**：13 节点，含 `Send` 并行扇出（多专家会诊）+ 条件路由（高风险触发 HITL）。
- ✅ **8 Agent 注册体系**：5 个有真实 Prompt 实现（家庭医生 / 护士 / 公卫 / 药师 / 助理），3 个为 stub（中医 / 营养 / 康复）。
- ✅ **Safety Guard 四层**：InputGuard（危机/注入检测）、OutputGuard（四态处置）、ActionGuard（动作风险分级）、HITL（人工审核队列）。
- ✅ **RAG 模块代码结构**：BM25 检索 + 重排 + 证据分级 + 安全过滤 + 受众分层 + 查询重写评估环路（代码齐全，但运行在降级模式，见 §3 P1）。
- ✅ **LLM 多模型分层路由**：6 类模型（lead / specialist / execution / vision / ocr / asr）+ 超时 + 指数退避重试。
- ✅ **多模态后端**：VisionService（图片）、SpeechService（语音）逻辑就绪，**但无 HTTP 上传入口**（见 §3 P2）。
- ✅ **18 个 SQLAlchemy 数据模型** + SQLite（开发自动建表）/ PostgreSQL（asyncpg，生产）双模式。
- ✅ **204 个测试用例全部通过**（`pytest tests/ -v`）。
- ✅ **Docker / docker-compose 基础配置**（存在缺陷，见 §3 P2）。
- ✅ **临床规则引擎**：风险分级、生命体征阈值、药物交互（DDI）—— 但仅几条硬编码示例规则（见 §3 P3）。

---

## 3. 真正需要做的（按优先级）

### P0 — 阻塞生产使用

| 项 | 现状 | 需要做 |
|----|------|--------|
| **用户认证体系** | `User` 模型存在，但**无认证中间件**，所有端点裸奔 | 实现 JWT / OAuth2，登录、鉴权、角色权限（医生 / 患者 / 管理员） |
| **患者 CRUD 完善** | 患者目前是「事件附带创建」，无独立管理入口 | 独立的患者增删改查、档案维护、与认证体系打通 |
| **Alembic 迁移脚本** | `alembic/versions/` **为空**（仅 `.gitkeep`） | 生成初始迁移；PostgreSQL 部署前必须补齐，否则无法管理 schema 变更 |
| **依赖锁定** | 无 `requirements.txt` / lock 文件，仅 `pyproject.toml` 范围约束 | 锁定可复现依赖版本（pip-tools / uv / poetry lock） |

### P1 — RAG 真正可用（工作量大）

| 项 | 现状 | 需要做 |
|----|------|--------|
| **灌入真实医学知识库** | `app/xuantong/rag/data/` 仅 **4 份示例文档**（高血压/糖尿病指南、用药安全、公卫服务） | 接入权威临床指南、药品说明书、公卫规范等真实知识源（含版权/合规审查） |
| **启用向量检索** | ChromaDB 代码已写（`chroma_retriever.py`）但**未启用**（`rag_vector_enabled=False`） | 部署 ChromaDB，开启混合检索（BM25 + 向量） |
| **部署 Embedding / 重排模型** | `sentence-transformers` 未安装，CrossEncoder 重排为**规则降级版** | 安装并部署 Embedding + reranker 模型（约需 2GB 显存） |
| **检索质量评估** | 无 recall / precision 指标，无 ground truth 测试集 | 构建评估集，建立检索质量基线与回归监控 |
| **知识更新机制** | 无增量更新、无版本管理 | 设计知识入库、版本化、失效与回溯流程 |

### P1 — Agent 智能真正可用

| 项 | 现状 | 需要做 |
|----|------|--------|
| **Prompt 工程迭代** | 当前 Prompt 为**初版**，未经真实临床数据打磨 | 用真实/仿真病例迭代，做临床合理性评审 |
| **Agent 评估体系** | 无 A/B 测试、无质量评分 | 建立输出质量评分、对比评测、回归基线 |
| **实现剩余 3 个 Agent** | 中医师 / 营养师 / 康复师**仅 stub** | 设计角色、Prompt、模型分配，并接入会诊流程 |
| **多轮对话能力** | 当前为**单轮问答**，无上下文记忆 | 引入会话记忆 / 状态管理（`memory/` 目录现为空壳） |

### P1 — 前端开发

- **完全从零开始**：仓库内**无任何前端代码**。
- 需要的界面：患者管理、医生工作台、事件上报、任务看板、时间线展示。
- 技术选型**待定**（React / Vue / 微信小程序等，需结合落地场景决策）。

### P2 — 实时通信与多模态

| 项 | 现状 | 需要做 |
|----|------|--------|
| **WebSocket / SSE** | 无 | 实时推送工作流状态变更（替代当前轮询 `/events/{id}/status`） |
| **文件上传端点** | Vision/Speech 后端就绪，但**无 HTTP 上传入口** | 增加图片/音频上传路由，串联多模态服务 |
| **多模态集成测试** | 代码写了但**未端到端验证** | 真实图片/语音样本的端到端联调 |

### P2 — 生产部署

| 项 | 现状 | 需要做 |
|----|------|--------|
| **Dockerfile 修正** | 含 `--reload`，且安装 `.[dev]`（开发依赖进生产镜像） | 生产镜像去掉 reload/dev，多阶段构建，非 root 用户 |
| **docker-compose 修正** | volume 路径 `./xuantong:/app` 可能有误（仓库根已是 xuantong） | 核对挂载路径，避免代码被空目录覆盖 |
| **CORS 收紧** | `allow_origins=["*"]` | 收敛到白名单域名 |
| **速率限制** | 无 | 接入限流（防刷、防 LLM 成本失控） |
| **结构化日志** | 无（标准 logging） | JSON 结构化日志 + 请求追踪 ID |
| **监控告警** | 无 | 指标采集、健康巡检、LLM 调用成本/失败率告警 |
| **CI/CD 流水线** | 无 | 自动化测试、构建、部署 |

### P3 — 业务深化

| 项 | 现状 | 需要做 |
|----|------|--------|
| **临床规则扩充** | 仅几条硬编码规则 | 接入真实临床指南，规则可配置化 |
| **药物交互数据库** | 示例数据 | 接入权威 DDI 数据源 |
| **随访管理** | `Followup` 模型存在，业务逻辑**不完整** | 随访计划生成、提醒、闭环跟踪 |
| **care-teams / health-records** | 端点存在但**返回空列表** | 补全照护团队组建与健康档案业务逻辑 |

---

## 4. 技术决策记录

- **为什么选 LangGraph 而非其他工作流引擎？**
  会诊场景需要「一个事件并行扇出给多个专家 Agent，再汇聚综合」，LangGraph 的 `Send` 原语天然支持动态并行扇出 + 条件路由（高风险走 HITL 分支），比线性 DAG 或纯代码 if-else 更易表达和可视化。配合 checkpointer 可支持中断恢复。

- **为什么 RAG 是 BM25-only 降级？**
  成本与依赖考虑。向量检索需要 Embedding 模型 + ChromaDB + 约 2GB 显存的 reranker，开发与测试环境负担重。当前用 BM25（字符级分词，适配中文）即可跑通检索流程。
  **升级条件**：安装 `sentence-transformers` + `chromadb`，将 `rag_vector_enabled` 置为 `True`，并补齐检索质量评估集。

- **模型分层策略（成本控制）**
  - `qwen3-max`：贵但强，**仅**主智能体（家庭医生，负责分析与综合决策）使用。
  - `qwen-plus`：会诊专家（护士/公卫/药师）使用，性价比平衡。
  - `qwen-flash`：便宜，执行类（家医助理）使用。
  - `qwen3-vl-flash` / `qwen3-asr-flash`：多模态（图片/语音）。
  目的：把算力花在刀刃上，避免每个 Agent 都用最贵模型导致成本失控。

- **Safety Guard 为什么分四层？**
  医疗场景安全责任分层、各司其职：
  - InputGuard 管「进来的」——危机信号（如自杀倾向）和注入攻击；
  - OutputGuard 管「出去给患者看的」——四态处置防止有害/不当内容直达患者；
  - ActionGuard 管「系统要做的动作」——按风险分级，决定能否自动执行；
  - HITL 管「拿不准的」——高风险/临界情况交人工审核。
  分层让安全策略可独立测试、独立演进，而非揉成一坨。

---

## 5. 已知坑点

- ⚠️ **SQLAlchemy UUID 列**：赋值时必须用 `UUID()` 包装字符串值，直接传字符串会报错。
- ⚠️ **BM25 小语料负分**：语料库 < 10 篇文档时，IDF 计算会产生**负分**，即使匹配成功。
  **不要**过滤 `score <= 0`（会导致小语料返回空）。正确做法：**min-max 归一化**所有分数，始终返回 top_k。
  代码位置：`app/xuantong/rag/knowledge_base.py` 的 `hybrid_search()`。
- ⚠️ **qwen-turbo 在北京地域不支持 Function Calling**：执行类 Agent **必须**用 `qwen-flash`，不要用 `qwen-turbo`。
- ⚠️ **Git 远程分支是 `master` 不是 `main`**：CI、部署脚本、PR 目标分支注意。
- ⚠️ **思考模式（enable_thinking）单独计价**：输出 token 价格约为普通的 **4-5 倍**，非必要不开启。当前仅家庭医生（`qwen3-max`）开启。

---

## 6. 参考项目说明

工作区 `c:\Users\LHT\Desktop\玄同\` 下另有 7 个参考项目，其核心思路已部分移植进玄同 RAG，但原始代码仍可作为进一步参考：

| 项目 | 参考价值 |
|------|----------|
| **LingYi** | 中医 RAG 参考（混合检索 + CrossEncoder 重排） |
| **_ref_drugclaw** | 药物 RAG 参考（证据分级 + 实体解析） |
| **medharness** | 医疗安全参考（PHI 脱敏 + 注入检测） |
| **MAS-in-diabetes** | 多 Agent 系统参考（受众分层） |
| **careflow** | 护理工作流参考 |
| **_ref_family-doctor** | 家庭医生 Agent 设计参考 |
| **Pipecat-agents** | 语音 Agent 参考（实时语音交互） |

> 移植到玄同 RAG 的模块包括：证据分级（`evidence.py`）、实体解析（`entity_resolver.py`）、受众分层（`audience_filter.py`）、安全过滤（`safety_filter.py`）、查询重写（`query_rewriter.py`）、评分（`grader.py`）等。

---

## 7. 工作量估算（给接手人的预期）

| 阶段 | 内容 | 估算 |
|------|------|------|
| **P0** | 认证 + 患者 CRUD + Alembic 迁移 + 依赖锁定 | 约 **1-2 周** |
| **P1** | RAG 知识库建设（含向量检索、Embedding、评估集） | 约 **2-4 周**（取决于知识源获取与合规） |
| **P1** | Agent Prompt 迭代 + 评估体系 + 剩余 3 Agent + 多轮对话 | 约 **2-3 周** |
| **P1** | 前端开发 | 约 **4-8 周**（取决于复杂度与选型） |
| **P2** | 实时通信 + 多模态上传 + 生产部署修正 | 约 **2-3 周** |
| **P3** | 业务深化（临床规则、DDI、随访、照护团队） | **持续性工作** |
| **合计** | 到 MVP 可用 | **约 2-3 个月（1-2 人全职）** |

> 估算为粗粒度参考，真实进度高度依赖：知识源可得性、临床专家评审投入、前端选型与范围。

---

## 8. 如何开始

### 环境与运行

```bash
# 1. 克隆（注意分支是 master）
git clone https://github.com/LHT666-hub/xuantong.git
cd xuantong

# 2. 安装（建议虚拟环境，Python >= 3.12）
pip install -e ".[dev]"

# 3. 配置环境变量
#    复制 .env.example 为 .env，填入百炼 API Key
#    没有 Key 时设 LLM_PROVIDER=mock 离线跑通

# 4. 跑测试，确认环境正常（应 204 passed）
pytest tests/ -v

# 5. 启动服务，访问 http://127.0.0.1:8000/docs
uvicorn app.main:app --reload
```

### 推荐的开发顺序

1. **先跑通现状**：装依赖 → 跑测试 → 启动服务 → 用 `/docs` 提交一个事件，观察工作流闭环（建议先用 `LLM_PROVIDER=mock`，零成本理解流程）。
2. **读核心代码**：`app/main.py`（装配）→ `app/xuantong/workflow/`（13 节点）→ `app/xuantong/agents/`（5 个实现）→ `app/xuantong/safety/`（四层）。
3. **补 P0**：认证 + 患者 CRUD + Alembic 迁移 + 依赖锁定 —— 这是任何生产化的前提。
4. **再攻 P1**：根据落地优先级，在「RAG 知识库」「Agent 智能」「前端」中选择切入；三者可并行（不同人）。
5. **P2/P3 持续迭代**：实时通信、多模态上传、生产部署、业务深化。

### 给接手人的几句话

- 测试全绿只代表**接口契约和流程贯通**，不代表临床正确性 —— 任何面向患者的输出都需临床专家评审。
- 改动 RAG 时务必记住 §5 的 **BM25 负分坑**，否则小语料会返回空。
- 涉及 LLM 的改动注意**成本**（模型分层 + 思考模式计价），别无意把所有 Agent 切到 `qwen3-max`。
- 有疑虑先查 [README.md](./README.md) 的架构图和本文 §4 的决策记录，理解「为什么这么设计」再动手。
